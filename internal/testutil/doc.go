// Package testutil provides shared test helpers for integration tests across
// the cardcore-server packages, including deterministic Hearts game fixtures,
// session config builders, and bearer token extraction.
package testutil

// Package flags provides small helpers for command-line flag parsing shared
// across the cardcore-server binaries. It does not define flags; it only
// bridges environment variables and Go's standard flag package.
package flags
